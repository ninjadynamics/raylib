#ifndef AUDIT002_GLKOS_H
#define AUDIT002_GLKOS_H

/* The focused host test discards every GLdc draw section at link time. The
 * production implementation supplies the two extension declarations itself;
 * this header only satisfies dc_mesh.c's platform include. */

#endif
